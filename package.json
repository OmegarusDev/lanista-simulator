{
  "name": "lanista-simulator",
  "version": "0.2.0",
  "private": true,
  "description": "Roman lanista autobattler — combat sandbox",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim:balance": "npx --yes tsx src/tools/balanceCli.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.2.0",
    "vitest": "^3.0.8"
  }
}
